# act-flexi Editor Environment
# Copy to .env for deployment

# Mode selection
# - development: Uses mock data and WebSocket sync
# - production: Full production build
# - github: GitHub Pages mode (localStorage sync)
MODE=development

# Remote WebSocket URL (editor only, for cross-origin dev)
# Leave blank in production
VITE_WS_URL=ws://localhost:5176
